name = "ccr"
main = "worker.js"
compatibility_date = "2024-09-07"
compatibility_flags = ["nodejs_compat"]

[vars]
# These are default values that can be overridden during deployment
# ANTHROPIC_PROXY_BASE_URL = "https://openrouter.ai/api"
# REASONING_MODEL = "google/gemini-2.5-pro"
# COMPLETION_MODEL = "google/gemini-2.5-pro"
# DEBUG = "0"

# Add secrets in the dashboard or using:
# wrangler secret put OPENROUTER_API_KEY

[build]
command = ""

[env.production]
name = "anthropic-proxy"